import { THead } from './TableHead.style';

export const TableHead = () => {
  return (
    <thead>
      <tr>
        <THead>Type</THead>
        <THead>Amount</THead>
        <THead>Currency</THead>
      </tr>
    </thead>
  );
};
